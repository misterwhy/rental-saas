<?php
// This is a Laravel Blade template file.
// It should be saved as 'resources/views/properties/index.blade.php'

// Extends the main application layout
?>
@extends('layouts.app')

<?php
// Defines the content section for this page
?>
@section('content')
<div class="content-wrap">
    <div class="content-head">
        <h1>Properties</h1>
        <div class="action">
            @auth
                @if(auth()->user()->isLandlord())
                    <a href="{{ route('properties.create') }}" class="save-btn">Add Property</a>
                @endif
            @endauth
        </div>
    </div>

    <div class="room-grid">
        @if($properties->count() > 0)
            @foreach($properties as $property)
                <div class="property-card">
                    <div class="card-header">
                        <div class="card-header-text">{{ $property->property_type }}</div>
                    </div>
                    <div class="image-container">
                        @if($property->main_image)
                            <img src="{{ asset('storage/' . $property->main_image) }}" alt="{{ $property->title }}">
                        @else
                            <div class="no-image">
                                <span>No Image</span>
                            </div>
                        @endif
                    </div>
                    <div class="card-details">
                        <div class="room-info">
                            <div class="room-type">
                                <svg class="bed-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M20 10V7c0-1.1-.9-2-2-2H6c-1.1 0-2 .9-2 2v3c-1.1 0-2 .9-2 2v5h1.5c.3 1.5 1.6 2.5 3 2.5s2.7-1 3-2.5h6c.3 1.5 1.6 2.5 3 2.5s2.7-1 3-2.5H22v-5c0-1.1-.9-2-2-2zm-9-3h2v3h-2V7zm-8 5h2v3H3v-3zm18 3h-2v-3h2v3z"/></svg>
                                {{ $property->bedrooms }} beds
                            </div>
                            <div class="room-price">
                                <svg class="price-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M11.8 10.9c-2.27-.59-3-1.2-3-2.15 0-1.09 1.01-1.85 2.7-1.85 1.78 0 2.44.85 2.5 2.1h2.21c-.07-1.72-1.12-3.3-3.21-3.81V3h-3v2.16c-1.94.42-3.5 1.68-3.5 3.61 0 2.31 1.91 3.46 4.7 4.13 2.5.6 3 1.48 3 2.41 0 .69-.49 1.79-2.7 1.79-2.06 0-2.87-.92-2.98-2.1h-2.2c.12 2.19 1.76 3.42 3.68 3.83V21h3v-2.15c1.95-.37 3.5-1.5 3.5-3.55 0-2.84-2.43-3.81-4.7-4.4z"/></svg>
                                <div class="price-value">${{ number_format($property->price_per_night, 2) }}/night</div>
                            </div>
                        </div>
                        <div class="property-address">
                            {{ $property->title }}
                        </div>
                        <div class="user-info">
                            <div class="user-name">
                                <svg class="user-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
                                <div class="user-name-text">{{ $property->landlord->name }}</div>
                            </div>
                            <div class="status">
                                <div class="status-text">Available</div>
                            </div>
                        </div>
                    </div>
                    <div class="editing-buttons">
                        <a href="{{ route('properties.show', $property) }}" class="view-details-btn">View Details</a>
                    </div>
                </div>
            @endforeach
        @else
            <div class="property-card">
                <div class="card-details">
                    <p>No properties found matching your criteria.</p>
                </div>
            </div>
        @endif
    </div>

    <div class="d-flex justify-content-center">
        {{ $properties->appends(request()->query())->links() }}
    </div>
</div>
@endsection
